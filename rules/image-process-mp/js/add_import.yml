id: add_import
language: javascript
rule:
  all:
    - pattern: let wasm
    - inside:
        kind: program
        stopBy: end
fix: |
    /* eslint-disable */
    import { TextDecoder } from '@/utils/encoding'

    let wasm;